## Пробы

- **Liveness**: `GET /health/live` — процесс жив.
- **Readiness**: `GET /health/ready` — модель загружена и процесс готов принимать трафик (инициализация выполняется при старте).
- **Startup**: отсутствует; загрузка модели происходит на старте приложения.
